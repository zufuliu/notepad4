// This file is part of Notepad4.
// See License.txt for details about distribution and modification.
//! Lexer for GeoGebra Script.

#include <cassert>
#include <cstring>

#include <string>
#include <string_view>

#include "ILexer.h"
#include "Scintilla.h"
#include "SciLexer.h"

#include "WordList.h"
#include "LexAccessor.h"
#include "Accessor.h"
#include "StyleContext.h"
#include "CharacterSet.h"
#include "StringUtils.h"
#include "LexerModule.h"

using namespace Lexilla;

namespace {

//KeywordIndex++Autogenerated -- start of section automatically generated
enum {
	KeywordIndex_Keyword = 0,
	KeywordIndex_Constant = 1,
	KeywordIndex_Innerfunction = 2,
	KeywordIndex_Function = 3,
	MaxKeywordSize = 36,
};
//KeywordIndex--Autogenerated -- end of section automatically generated

enum class KeywordType {
	None = SCE_GGB_DEFAULT,
	Return = 0x40,
};

constexpr bool IsSpaceEquiv(int state) noexcept {
	return state <= SCE_GGB_TASKMARKER;
}

constexpr bool IsGeoGebraNumberContinue(int chPrev, int ch, int chNext) noexcept {
	return ((ch == '+' || ch == '-') && (chPrev == 'E'))
		|| (ch == '.' && chNext != '.');
}
constexpr bool IsGeoGebraDecimalNumber(int chPrev, int ch, int chNext) noexcept {
	return IsIdentifierChar(ch) || IsGeoGebraNumberContinue(chPrev, ch, chNext);
}

void ColouriseGgbDoc(Sci_PositionU startPos, Sci_Position lengthDoc, int initStyle, LexerWordList keywordLists, Accessor &styler) {

	int lineStateLineType = 0;
	KeywordType kwType = KeywordType::None;

	int visibleChars = 0;
	int chPrevNonWhite = 0;

	StyleContext sc(startPos, lengthDoc, initStyle, styler);

	while (sc.More()) {
		switch (sc.state) {
		case SCE_GGB_OPERATOR:
			sc.SetState(SCE_GGB_DEFAULT);
			break;

		case SCE_GGB_NUMBER:
			if (!IsGeoGebraDecimalNumber(sc.chPrev, sc.ch, sc.chNext)) {
				sc.SetState(SCE_GGB_DEFAULT);
			}
			break;

		case SCE_GGB_IDENTIFIER:
			if (!IsIdentifierChar(sc.ch)) {
				char s[MaxKeywordSize];
				sc.GetCurrent(s, sizeof(s));
				if (keywordLists[KeywordIndex_Keyword].InList(s)) {
					sc.ChangeState(SCE_GGB_KEYWORD);
				} else if (keywordLists[KeywordIndex_Constant].InList(s)) {
					sc.ChangeState(SCE_GGB_CONSTANT);
				} else if (keywordLists[KeywordIndex_Innerfunction].InListPrefixed(s, '(')) {
					sc.ChangeState(SCE_GGB_INNERFUNCTION);
				} else if (keywordLists[KeywordIndex_Function].InListPrefixed(s, '(')) {
					sc.ChangeState(SCE_GGB_FUNCTION);
				}
				sc.SetState(SCE_GGB_DEFAULT);
			}
			break;

		case SCE_GGB_COMMENTLINE:
			if (sc.atLineStart) {
				sc.SetState(SCE_GGB_DEFAULT);
			}
			break;

		case SCE_GGB_STRING:
			if (sc.ch == '\"') {
				sc.ForwardSetState(SCE_GGB_DEFAULT);
			}
			break;
		}

		if (sc.state == SCE_GGB_DEFAULT) {
			if (sc.ch == '#') {
				sc.SetState(SCE_GGB_COMMENTLINE);
				if (visibleChars == 0) {
					lineStateLineType = SimpleLineStateMaskLineComment;
				}
			} else if (IsNumberStartEx(sc.chPrev, sc.ch, sc.chNext)) {
				sc.SetState(SCE_GGB_NUMBER);
			} else if (IsIdentifierStart(sc.ch)) {
				chPrevNonWhite = sc.ch;
				sc.SetState(SCE_GGB_IDENTIFIER);
			} else if (sc.ch == '\"') {
				sc.SetState(SCE_GGB_STRING);
			} else if (isoperator(sc.ch) || sc.ch == '$') {
				sc.SetState(SCE_GGB_OPERATOR);
			}
		}

		if (!isspacechar(sc.ch)) {
			visibleChars++;
			if (!IsSpaceEquiv(sc.state)) {
				chPrevNonWhite = sc.ch;
			}
		}


		if (sc.atLineEnd) {
			styler.SetLineState(sc.currentLine, lineStateLineType);
			lineStateLineType = 0;
			visibleChars = 0;
			kwType = KeywordType::None;
		}
		sc.Forward();
	}

	sc.Complete();
}

struct FoldLineState {
	int lineComment;
	int moduleImport;
	constexpr explicit FoldLineState(int lineState) noexcept:
		lineComment(lineState & SimpleLineStateMaskLineComment),
		moduleImport((lineState >> 1) & 1) {
	}
};

}

extern const LexerModule lmGeoGebra(SCLEX_GEOGEBRA, ColouriseGgbDoc, "GeoGebra", FoldSimpleDoc);
